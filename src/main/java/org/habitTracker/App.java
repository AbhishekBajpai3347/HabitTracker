package org.habitTracker;
import org.habitTracker.controller.ConsoleController;

public class App
{
    public static void main( String[] args )
    {
        ConsoleController app = new ConsoleController();
        app.AppRunner();
    }
}